{{#if route.linkable}}
  {{#link-to route.path class=linkClass}}
    {{#if hasBlock}}
      {{yield this route}}
    {{else}}
      {{i18nTitle}}
    {{/if}}
  {{/link-to}}
{{else}}
  {{#if hasBlock}}
    {{yield this route}}
  {{else}}
    {{i18nTitle}}
  {{/if}}
{{/if}}
